# Handoff, fim de 26/08/2026

> Cole isto no início da sessão nova. Ele é curto de propósito: aponta para os
> arquivos em vez de repetir o conteúdo deles.

## Regra desta sessão

**Leia os arquivos antes de perguntar.** Quase toda pergunta que surgir já está
respondida em `docs/` ou `specs/`. Abra o arquivo, não reconstrua o raciocínio.

## O que mudou de rumo, e é a coisa mais importante

**A prioridade inverteu em 26/08.** Toda especificação já definida é implementada
**na Lovable** (`nexclin/nexclin`), e a stack nova (`nexclin/nexclin-sdd`) espera.
Migração gradual em setembro, oficial em outubro.

Isso contraria o que a §2.5 do `CLAUDE.md` vinha orientando na prática. A razão e
o custo estão em `docs/planejamento/inversao-de-prioridade-26-08.md`. **Não
reabra essa discussão.**

## Onde ler, em ordem

| Arquivo | Para quê |
|---|---|
| `docs/planejamento/registro-26-08.md` | O que foi feito em 26/08, commit a commit |
| `docs/planejamento/inversao-de-prioridade-26-08.md` | Por que a Lovable passou na frente |
| `specs/006-modelagem-ini/spec.md` | Os dez requisitos implantados e por que cada decisão foi tomada |
| `docs/ponte/blocos-26-08/README.md` | As seis migrações a colar, bloco a bloco |
| `docs/planejamento/verificacao-modelagem-26-08.md` | A bateria de verificação e os três defeitos que ela achou |
| `.claude/rules/escrita.md` | Como este projeto escreve. Travessão proibido |

## O que o Arthur tem de executar, e ele já está em casa

> **Atualizado em 27/08:** os itens 1 e 2 abaixo foram feitos. As seis
> migrações estão aplicadas, com `true` nas onze linhas da conferência, e tudo
> está publicado na Lovable, que está em `7945875`. **Comece pelo item 3.**

**1. ~~Colar as seis migrações.~~ Feito em 27/08.** A conferência devolveu `true`
nas onze linhas. Dois resultados que valem saber:

- `encerra_impersonacoes_vencidas()` devolveu **0**: o furo da impersonação
  existia e nunca foi acionado. Agora tem prazo de duas horas.
- O reparo da linha do tempo cobriu **27 ações auditadas** que a tela de detalhe
  da conta mostrava vazias. Depois dele, zero pendentes.

**2. ~~Publicar os commits pendentes.~~ Feito em 27/08.** Local e remoto em
`7945875`, 76 testes passando, a edge function de MCP intacta com 239 linhas.

**3. Trocar a senha da conta-mestra** por recovery no painel do Supabase. Foi
exposta em chat pela segunda vez. Registro em
`docs/seguranca/credencial-exposta-2026-08-26.md`.

**4. Ligar SMTP (Resend)** em Authentication → Emails → SMTP Settings. Sem ele
não sai reset de senha nem convite de equipe, e o superadmin da stack nova nunca
logou.

**5. Transcrever os apontamentos do Erick**, que ele gravou em vídeo. **Bug de
quem testou tem precedência sobre funcionalidade nova.**

## O que está pendente e não depende dele

| Pendência | Onde |
|---|---|
| Centros de custo | Único item "importa" da modelagem ainda não implantado |
| Reteste do Vinícius | Os 23 itens estão corrigidos e publicados desde 25/08 |
| Emenda D-005.5, `consultas` sai do contrato | Aprovada, aplicação na fase de migração |
| Auditoria do que o suporte cria sob impersonação | Dívida registrada na SPEC 006 |
| Aceites das SPECs 001, 003 e 005 | Bloqueados pelo login do superadmin |

## Como trabalhar aqui

**Na Lovable**, o gate é `npx tsc --noEmit -p tsconfig.app.json`, nunca `npm run
build`. E há 76 testes: `npx vitest run`.

**Nunca `git add -A` no repositório da Lovable.** O pacote `@lovable.dev/mcp-js`
tem um gerador que reescreve `supabase/functions/mcp/index.ts` no `npm install` e
no `vite build`, reduzindo o arquivo de 239 linhas para 2. Aconteceu em 26/08.
Adicione por caminho.

**Migração nasce em `nexclin-sdd/supabase/migrations`**, que é a fonte de verdade
do schema, e só depois vai para a plataforma. Nenhum agente aplica migração no
editor de SQL da Lovable: em 19/08 ficou provado que ele executa consulta
diferente da que está na tela quando dirigido por automação.

**Ao escrever documento com crase**, use arquivo e não `python -c`, senão o bash
executa as crases como comando. Aconteceu três vezes em 26/08.

## O padrão do código novo

Toda funcionalidade da modelagem tem um **módulo puro** sem banco, sem relógio e
sem navegador, mais a tela. O relógio é parâmetro, porque função que lê o relógio
não se testa. Os módulos estão em `src/lib/` da Lovable, e os testes em
`src/lib/__tests__/`.

`src/lib/hoje.ts` é a base: devolve o dia do calendário brasileiro. Todas as
telas com data passam por ele.

## O que NÃO fazer

- Não construir na stack nova até a Lovable estar pronta.
- Não criar ModuleKey nova sem emenda à constituição.
- Não escrever `clinic_id` pela aplicação: as tabelas têm `DEFAULT
  get_my_clinic_id()`.
- Não aplicar migração por automação.
- Não usar travessão em texto nenhum.
