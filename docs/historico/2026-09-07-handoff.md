# Handoff de 07/09/2026, madrugada

**Vespera do lancamento.** Escrito para a sessao seguinte, e para o Arthur
saber o que publicar.

## 1. O que subiu para o `main` de `nexclin/nexclin`

Quatro commits novos, todos com o gate de tipos do `tsconfig.app.json` em zero
erros, todos **sem publicar**. Com eles sao **vinte e dois commits pendentes**.

| Commit | O que corrige | Quem sente |
|---|---|---|
| `abbc3c8` | Insights e Fluxo de Caixa param de ler `revenues` | **toda clinica real**, no Insights |
| `0012e12` | a tarefa criada pelo motor se declara `automatica` | ninguem hoje, o dado em outubro |
| `d10eeb6` | Saldo do Periodo deixa de ignorar o que veio antes do periodo | quem opera ha mais de um mes |
| `c43a640` | o DFC/DRE usa o valor que o banco creditou | ninguem hoje, todos na primeira baixa |

O detalhe de cada um esta em
[`2026-09-07-v27-duas-telas-que-ficaram-de-fora.md`](2026-09-07-v27-duas-telas-que-ficaram-de-fora.md)
e nas mensagens dos proprios commits.

## 2. O aceite, que e do Arthur e fecha os tres

Depois do Publish, na clinica povoada:

1. **Insights**: gerar um insight. O faturamento do retrato tem de deixar de
   ser R$ 0.
2. **Fluxo de Caixa**: o total de entradas do mes tem de **cair perto da
   metade**, porque era exatamente a duplicacao. **Se nao cair, a hipotese
   estava errada e o documento precisa de emenda.**
3. **Saldo do Periodo**: tem de **subir**, e passar a bater com abertura mais
   recebido menos gasto ate a vespera do periodo.
4. **Tarefas**: criar uma consulta e conferir no SQL editor que a tarefa de
   confirmacao nasce `origem = 'automatica'`, e que uma tarefa do formulario
   nasce `'manual'`.

Lembrete do procedimento, que ja custou uma publicacao vazia: se o editor
disser **"Preview is out of date"**, clique **Update preview** e espere
terminar. Publicar com preview velho sobe o codigo antigo e ainda responde
"up to date".

## 2b. A segunda leva, ainda de 07/09

| Commit | O que corrige |
|---|---|
| `9e3e0f1` | a barra de rolagem da barra lateral some, e o Fluxo de Caixa ganha calendario |
| `06cd11b` | abrir a barra deixa de mover os icones |
| `38a1f05` | o valor recebido para de sumir depois da baixa |

**A barra de rolagem era ordem de cascata, e por isso duas tentativas
anteriores falharam.** O arquivo pedia `scrollbar-width: none` duas vezes e,
mais abaixo, um bloco de 27/08 pedia `thin` no mesmo seletor. Mesmo peso, entao
vencia a ultima. Medido no publicado: computado `thin`, barra ocupando 10px.

**Os icones subiam por duas causas somadas**, as duas medidas: o item media
36px recolhido contra 28px expandido, e o `padding-bottom` do cabecalho valia
24px contra 10px, que sao os 14px de salto do topo da nav. Os degraus por
altura de janela tambem so valiam no estado recolhido, e recriariam o salto em
tela de 720px ou mais. Controle positivo depois da correcao, injetada no
publicado: `deltaCy [0,0,0,0]`, `deltaNavTop 0`.

**Aceites destes tres, e sao do Arthur:**

5. Abrir e fechar a barra: nenhum icone se move, e barra de rolagem nenhuma
   aparece mesmo com o grupo Financeiro aberto.
6. Fluxo de Caixa: escolher uma semana no calendario e conferir que grafico e
   tabela passam a mostrar so aquele intervalo, e que "Voltar ao mes" desfaz.
7. Baixar R$ 100 recebendo R$ 97: a lista de Contas a Receber mostra 100 com
   "recebido R$ 97,00" embaixo, e o Total Liquido do relatorio usa 97.

## 2c. A terceira leva, e o Arthur publicou entre a segunda e a terceira

| Commit | O que entrega |
|---|---|
| `769953e` | a taxa de antecipacao vem do cadastro, e nao da mao |
| `7e98c98` | faxina de Consultas: treze cartoes viram treze linhas |
| `e12d8cc` | Recall sai do Financeiro e vai para Operacao |
| `8c93fc7` | **Realizado e Previsto viram dois modos, e Realizado e o padrao** |
| `7962a1c` | mes e ano saem do Fluxo de Caixa, o calendario fica sozinho |

**O `8c93fc7` e o mais importante do dia, e nasceu de uma correcao do Arthur.**
Eu tinha reportado que a tela "escondia R$ 162 mil de vencido". Ele devolveu:
aquilo e promessa, e nao caixa, e paciente que agendou pode nao comparecer. O
enquadramento estava errado, e a resposta certa era pior do que o achado: a
tela nao era nem realizado nem previsto, e a fronteira entre os dois era o
acaso da janela da consulta. Virou o FR-002c da regra 021.

**O que esperar ao publicar**, na clinica NexClin, em setembro:

| Modo | Entradas |
|---|---|
| Realizado | perto de R$ 0,00, porque nada foi baixado em setembro |
| Previsto | cerca de R$ 201,5 mil, sendo R$ 162 mil de vencido |

O realizado quase zerado **nao e defeito**, e o dado: o povoamento vai ate
agosto. Em clinica de verdade ele sobe a cada baixa.

## 3. O achado que ficou aberto, e foi fechado no mesmo dia

**Duas familias de telas financeiras contam com regras diferentes de valor.**

| Usa `valorDeCaixa`, ou seja prefere `settled_value` | Usa `net_value` cru |
|---|---|
| Dashboard, Fluxo de Caixa, Insights | Contas a Receber, Relatorio Contas a Receber, Relatorio DFC/DRE, Relatorio de Vendas, `useFinancialBreakdown` |

Hoje as duas familias **concordam**, e por um motivo que expira: o censo de
06/09 mediu **zero baixas gravadas**, entao `settled_value` e nulo em toda
linha e o `valorDeCaixa` cai no `net_value`. **Na primeira baixa em duas etapas
que um fundador der, as duas familias divergem.**

E exatamente o cenario da issue #72: baixar R$ 100 recebendo R$ 97. O painel
passa a dizer 97 e os relatorios continuam dizendo o previsto.

**Nao foi corrigido de proposito.** Nao e um bug de uma linha: e uma decisao por
tela, e cada uma tem resposta propria.

- **Relatorio DFC/DRE**: e demonstrativo de fluxo de caixa. Tem de usar o que
  entrou, ou seja `settled_value`. Era o unico caso sem ambiguidade, e por isso
  **ja foi corrigido em `c43a640`**: a regra 021 decide por ele, entao nao
  precisava de decisao nova. Hoje o numero nao muda, porque `settled_value` e
  nulo em toda linha; a correcao existe para nao divergir na primeira baixa.
- **Relatorio de Vendas**: vendeu R$ 100. O previsto defende-se sozinho.
- **Contas a Receber, tela e relatorio**: sao carteira. O previsto e o certo na
  coluna de previsto; o que falta e uma coluna de recebido que use o
  `settled_value`.

**Recomendacao:** corrigir o DFC/DRE, e tratar o resto como coluna nova, que e
requisito da stack nova e nao conserto de vespera. A decisao e do Arthur.

### Fechado em 07/09, e o Arthur mandou prosseguir

As tres telas foram tratadas, cada uma pela sua resposta:

| Tela | O que ficou |
|---|---|
| Relatorio DFC/DRE | usa `valorDeCaixa`, em `c43a640`. Demonstrativo de caixa responde quanto entrou |
| Relatorio Contas a Receber | coluna e total liquidos usam `valorDeCaixa`, em `38a1f05` |
| Contas a Receber, a tela | a coluna Valor segue no previsto, e o recebido aparece embaixo quando diverge, em `38a1f05` |
| Relatorio de Vendas | **nao mudou, de proposito.** Vendeu R$ 100, e o previsto e a resposta certa |
| `useFinancialBreakdown` | **nao estava na divergencia.** Seleciona `net_value` e soma `gross_value` |

**Nenhum numero muda hoje.** `settled_value` e nulo em toda linha, porque o
censo de 06/09 mediu zero baixas, e a cadeia cai no `net_value` de antes. As
quatro superficies passam a concordar a partir da primeira baixa em duas
etapas, que e a issue #72.

## 4. O que continua parado, e por que

- **O SQL da comparacao linha a linha** das 280 de `revenues` contra as 280 de
  `receivables`. Deixou de ser urgente: a correcao do Fluxo de Caixa vale
  independente de as linhas parearem uma a uma. Continua sendo insumo do
  portao 1 (#60), que e decisao pos-lancamento.
- **Os numeros do dashboard conferidos contra o banco**, tela a tela. Depende de
  abrir a clinica, e agora tres numeros mudaram, entao a conferencia so faz
  sentido **depois** do Publish.
- **A fase 1 da 022**, por decisao de 06/09: vai para outubro, e o artefato
  desta semana e a regra, ja escrita.
- **O #50**, o segundo usuario. Continua sendo a tarefa que destrava mais coisa
  por menos esforco: cinco provas de permissao, o FR-011 e a foto do
  responsavel.

## 5. Registro de honestidade

Nenhuma das tres correcoes foi vista funcionando numa tela. O que existe e
leitura de codigo, o conteudo do povoamento, a ausencia de escrita em
`revenues` provada com dois controles positivos, e o gate de tipos em zero
erros. Pela alinea (j): **codigo lido, nao comportamento provado**, e os quatro
aceites da secao 2 sao o que fecha.
