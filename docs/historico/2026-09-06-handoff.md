# Handoff de 06/09/2026

Escrito para a sessão seguinte começar sabendo de tudo sem reler a conversa.
**Faltam dois dias para 08/09.** O limite semanal de token estava em 61% usado
no fim desta sessão, e foi por isso que ela terminou aqui.

---

## 1. O estado em uma tela

**Dezesseis commits estão no `main` de `nexclin/nexclin` e NENHUM foi
publicado.** Publicar é do Arthur, pela ponte inversa. Do mais novo para o mais
antigo:

| Commit | O que entrega |
|---|---|
| `896cd94` | acentos nas mensagens ao paciente, e a barra lateral cresce em tela grande |
| `c6191b6` | o sino de notificações |
| `d00eafa` | Recall com o desenho da cobrança, e Configurações de volta à barra |
| `9d39c06` | **baixa em duas etapas em Contas a Receber e a Pagar** |
| `ed9cb46` | o selo de prazo que pisca de verdade |
| `537016b` | Saldo atual do fluxo de caixa, refeito porque estava errado |
| `b708283` | contagem regressiva, aba Leads igual ao funil, linhas finas, cobrança densa |
| `d68d949` | cabeçalho da cobrança |
| `a96861a` | Consultas: pastilhas e responsável com foto |
| `d8f0fff` | linha do cabeçalho sai, coluna do funil vira caixa |
| `0dad6af` | Tarefas: responsável primeiro, atraso, pisca |
| `f68afb7` | Saldo atual, primeira versão |
| `060c73c` | barra lateral e centavo do herói |
| `6c6a1f2` | **auditoria dos números do dashboard** |

No `nexclin-sdd`, tudo na branch `claude/meeting-priorities-next-steps-qxktqd`,
que ainda tem o **PR #54 aberto**.

## 2. O que foi aplicado no banco ao vivo, e o que provou

O Arthur rodou o **bloco 1 da fase 1 da regra 021** em 06/09
(`docs/ponte/aplicacao-021-fase1/`). Ele criou seis colunas:
`settled_value`, `settled_by` e `settled_at` em `receivables` e em `expenses`.

A conferência devolveu `intactas` igual a `linhas`: **604 e 604** em
`receivables`, **157 e 157** em `expenses`, e zero baixas gravadas. Como a
consulta cita as colunas pelo nome, coluna faltando daria erro e não linhas.
**As issues #61 e #62 estão fechadas por isso.**

## 3. Os dois achados que mudam o plano

**O primeiro derrubou uma afirmação da regra.** O FR-004 da 021 dizia que
`bank_accounts` não tem coluna de saldo. **É falso.** `opening_balance` e
`opening_date` existem desde `20260427222514`, de 27/04, e o front já as lê em
três lugares. A afirmação veio de uma busca que parou no `CREATE TABLE` e não
alcançou o `ALTER TABLE`. Sem conferir, uma migração desnecessária teria ido
para o banco ao vivo a três dias do lançamento. A regra foi corrigida.

**O segundo explica a foto do responsável.** O censo devolveu **um usuário só**
na clínica de teste, e as tarefas estão atribuídas a **setores**, "Comercial" e
"Financeiro", não a pessoas. Por isso a foto cai nas iniciais. **Não é bug de
código.** É o dado, e é o que o FR-005 da regra 022 existe para encerrar.

## 4. O que está aberto, em ordem

### 4.1 Do Arthur, e destrava o resto

1. **#50, convidar um segundo usuário.** É a tarefa mais barata da fila e a que
   destrava mais: cinco provas de permissão, o FR-011, e a foto do responsável.
   Passo a passo em `docs/ponte/50-segundo-usuario-passo-a-passo.md`.
2. **#78, o bloco 1 do censo de tarefas.** Dá o número que libera a fase 1 da
   022: quantos valores de `responsible` não casam com membro.
3. **#58, os blocos 3, 3b e 3c do censo financeiro.** Sem eles o portão 1 (#60),
   que decide o destino de `revenues`, fica sem base.
4. **Publicar** os dezesseis commits e conferir o marcador dentro do bundle.
5. **Aceitar**: #71, #72 e #73 são o roteiro das telas de baixa.

Já marcadas OK por ele em 06/09: **#47** restauração testada e **#48** senha do
superadmin.

### 4.2 Do Claude, e nesta ordem

1. **#80 a #83**, a migração da 022 mais o bloco guiado. **Espera o #78.**
2. **#87**, o front gravando o responsável como usuário. **Espera a migração.**
3. **`initialBalance` do Saldo do Período.** Ele ignora todo movimento anterior
   ao período, então o número só está certo quando a abertura da conta coincide
   com o início do período. **Achado, não corrigido, porque muda número que o
   Arthur já olha e a decisão é dele.**
4. **#40**, soft delete de paciente. LGPD, e espera a migração #39.
5. **A escolha da regra 023**: comentário na tarefa, ou caixa de mensagens com
   emenda à constituição. **A recomendação é o comentário**, e nos dois casos
   depois de 08/09.
6. **#38** `public_token`, e **#45** o backport, que só importa em outubro.

### 4.3 Sabido e não consertado

- A barra lateral ainda rola abaixo de **657px** de janela. Medido, e o Arthur
  mandou deixar para depois.
- `CONTEXT.md` tem 73 linhas contra um teto de 60 que ele mesmo declara.
- O sino **não respeita permissão de módulo**, por causa do FR-011.

## 5. Como o Arthur acessa o banco

**O dado ao vivo NÃO está num projeto Supabase comum: está no Lovable Cloud**,
e por isso não aparece no painel do Supabase. Foi um achado de 05/09, e sete
documentos diziam "Supabase Pro" sem nomear projeto nenhum.

O caminho que ele já usa, e que funcionou em 06/09:

1. abrir o projeto na **Lovable**;
2. abrir o **backend do projeto**, onde fica o editor de SQL;
3. colar o bloco, rodar, e **exportar o resultado em CSV** pelo botão de export
   do próprio editor;
4. mandar o CSV no chat.

**O passo 2 tem uma armadilha registrada:** logo abaixo do `Export data` ficam
`Pause` e `Remove`, os dois em vermelho, num espaço de cerca de 200 pixels.

> **Para o Arthur completar:** a URL exata do editor. Ela não está escrita em
> lugar nenhum do repositório, e inventar link de painel de produção é pior que
> deixar em branco.

O Claude **não alcança** esse editor: o proxy desta sessão bloqueia
`supabase.com` e `ini.app.br`. Todo SQL chega como bloco para colar, e todo
resultado volta como CSV.

## 6. O que reger qualquer decisão

- **§2.5 do `CLAUDE.md`:** a Lovable é ponte, não destino. Corrigir lá só vale
  quando atravessa. Faixa A é banco, faixa B é regra escrita, faixa C não se
  corrige. **Exceção com nome: relatório e financeiro.**
- **A régua fina:** dado atravessa, cálculo de tela não. Erro de lançamento em
  setembro **não é descartado em outubro, é importado**.
- **Alínea (j):** quando não deu para provar, escreva *código lido, não
  comportamento provado*, e deixe aberto.
- **Toda afirmação negativa precisa de controle positivo.** Nesta sessão o
  controle positivo salvou duas vezes: reprovou uma sonda de barra de rolagem
  que media zero sempre, e reprovou um `clamp` que parecia certo e crescia o
  menu em janela pequena.
