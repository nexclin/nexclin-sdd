# Handoff, 15/09/2026, segunda sessão

Para a sessão seguinte continuar sem reler a conversa. Atualiza
`2026-09-15-auditoria-024-escopo-e-business-rules.md` e o handoff de 14/09.
Só o que não está escrito em outro lugar; o resto por caminho.

## Onde as coisas moram

| O que | Onde |
|---|---|
| Ramo vivo | `claude/handoff-nexclin-2026-09-15-e68c0e`, no `origin`. Contém tudo de `claude/handoff-nexclin-2026-09-11-84de05` mais este dia. **Nenhum dos dois está em `main` do `nexclin-sdd`**; links das issues (`blob/main/`) só resolvem depois do merge, que é decisão do Arthur |
| Clone da Lovable | `../nexclin-lovable`, `main` = `origin/main` em `b44a164` |
| Issues | `nexclin/nexclin`, milestones 021 a 024. Todo `gh issue` leva `-R nexclin/nexclin` |

## O que fechou hoje

- **Publish provado.** Bundle `index-C_kw4h5d.js` com marcadores de `05edccd`
  e `c26d645`. `.ponte-bundle` atualizado. Gates: `tsc` limpo, 300 testes.
- **Issues fechadas (26):** 024 T321, T331 a T333, T337, T345, T346, T351,
  T359, T362, T369, T372 a T376; 023 T224, T235, T239, T244, T246 a T249.
- **Provas de tela e de editor** (16 de tela, 4 de editor) receberam o
  comentário "código lido, não comportamento provado" com arquivo e linha, e
  **continuam abertas**.
- **Auditoria T374:** sem nível alto. Um médio confirmado, `business_rules`
  aceitava escrita de qualquer membro (nexclin#185).
- **`speckit-analyze` nas duas frentes**, e o crítico e os altos corrigidos
  no mesmo dia a pedido do Arthur: migração `20260915020000` (policy nova de
  `business_rules` e função `tarefas_assumidas_por_membro`), copiada para
  `docs/ponte/aplicacao-024-fase1/b2-*.sql`; front em `b44a164` da Lovable.
  Regras 023 e 024 precisadas pela alínea (l).
- **Issues abertas hoje:** nexclin#184 (policy de escopo na stack nova, T376)
  e nexclin#185 (`business_rules`).

## O que é do Arthur, em ordem

1. **Editor de SQL, `Clear` antes de colar.** Nesta ordem:
   `aplicacao-024-fase1/b2-business-rules-e-assumidas.sql`, depois
   `b2-conferencia.sql` e `b2-prova.sql` (fecha #185 e prova FR-011);
   `aplicacao-023/b6-realtime.sql` e `b6-conferencia.sql` (T242, #45);
   `aplicacao-024-fase1/b1-prova-t320-t221.sql` (T320, T221 a T223);
   `aplicacao-024-fase1/t307-segundo-usuario.sql` (T307, T206; resultado vai
   para `docs/historico/` sem e-mail).
2. **Publish** de `b44a164`, **depois** do `b2` no banco. Sem a função, o
   ranking mostra "indisponível" na coluna de assumidas.
3. **Provas de tela**, com Maria e Dr. Lançamento em abas: 024 T334 a T336,
   T347 a T350, T360, T361, T368, T370, T371; 023 T236 a T238, T245. Cada
   issue tem o que foi lido e o que falta ver.
4. **Decisão T352** (#101): os dois caminhos estão na issue, com
   recomendação pelo caminho 1 (migração a partir de `profiles`).
5. **Merge do ramo em `main`.**

## O que trava

O classificador do modo automático recusa **qualquer** SQL no editor de
produção, inclusive `SELECT` (tentado em 15/09 para T307). Não contornei. Todo
SQL vai por arquivo em `docs/ponte/`, e o Arthur cola.

## Aberto, e importa

1. Claros Clinic e Cezar Essence não aparecem em `clinics.name` (censo 14/09).
   Perguntar antes de qualquer aceite com clínica real.
2. Nomes de migração em T207 e T241 do `tasks.md` da 023 divergem dos
   arquivos reais (`20260914020000` e `20260915010000`). Tarefas fechadas;
   fica só o registro.
3. `types.ts` da Lovable tem `tarefas_assumidas_por_membro` editado à mão. Se
   o bot regenerar, some.
4. A frente 021 e 022 têm 58 issues abertas, quase todas stack nova ou portão
   do Arthur (T021, T126 a T129). Nada delas foi tocado hoje.

## Ambiente

- Duas rodadas de `vitest` ao mesmo tempo derrubam quatro testes de data por
  timeout. Uma de cada vez: 300 verdes.
- `gh` às vezes trava sem erro; `timeout 90 gh ...` resolve.
- Arquivos `.md` e `.sql` são CRLF; `autocrlf=true`. Node com
  `replace(/\r\n/g,"\n")` e volta.

## Adendo, mesma noite: 021 e 022

**O que o Arthur rodou:** `b2` (business_rules e `tarefas_assumidas_por_membro`)
e `b6` (Realtime), com sucesso relatado; resultado da `b2-prova` não colado.
**Seed** para o painel de Maria em
`docs/ponte/aplicacao-024-fase1/seed-clinica-lancamento.sql`, com limpeza ao
lado; pedido dele para julgar layout e a caixa de mensagens.

**Estado real das duas frentes, que o `tasks.md` não dizia:** as issues
`#55` a `#100` do `nexclin-sdd` foram fechadas em lote em 14/09 e não viraram
issue nova. Reconstruído pelos handoffs de 06 e 07/09 e pelo `git log` da
Lovable: 021 Fase 1 no ar (`9d39c06`, banco 06/09); 022 Fase 1 superada pela
decisão 1b da seção 7 e coberta pela 024; 022 Fase 2 no ar (`0dad6af`,
`a7ce299`). Provas de tela de ambas não feitas. Marcado no `tasks.md` com a
evidência em cada linha.

**Portões, todos com os dois lados escritos na issue:** T021 (#127, FR-011),
T126 (#181, papel), T127 (#182, janela), T128 (#183, checklist). **O achado
novo de T021:** `Acompanhamento.tsx` insere e apaga `receivables` sob o módulo
`acompanhamento`; policy de `receivables` só por `contas_receber` quebraria a
venda da secretária (024 FR-004 e FR-005). A forma proposta está na issue. A
migração do FR-011 não foi escrita, de propósito: a forma depende da decisão.

**022 F6 cruzada com 023 e 024:** T147 fechada (`task_comments`), T150 e T152
fechadas; T149, T151 e T153 esperam a metade de `my_permission`.

**O que resta nas duas frentes é stack nova** (021 F3 a F5: extrato, OFX,
conciliação, recorrência, tela; 022 F3 a F5: motor, papel, tela da manhã) ou
portão do Arthur. Pergunta feita a ele: essas fases sobem na Lovable, pela
exceção do Princípio IV como a 023 e a 024, ou ficam para outubro como os
planos dizem?
