# === Segredos (constituição, Princípio V — NUNCA versionar) ===
.env
.env.local
.env.*.local
*.key
*.pem

# === Node / Next.js ===
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# === Supabase (CLI local) ===
supabase/.temp/
supabase/.branches/
.supabase/

# === Testes ===
coverage/
playwright-report/
test-results/

# === OS / editor ===
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# === Spec Kit — credenciais que o agente possa guardar ===
.claude/.credentials*

# === Agent Skills instaladas (terceiros — como deps) ===
.agents/
